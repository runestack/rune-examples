secret:
  name: db-credentials
  namespace: demo
  type: static
  data:
    username: notes
    # Demo only — override per environment:
    #   rune cast ./runeset --release notes --set postgres.password=...
    password: {{ values:postgres.password | default: change-me-please }}
